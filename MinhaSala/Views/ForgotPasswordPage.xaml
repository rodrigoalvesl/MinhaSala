<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MinhaSala.Views.ForgotPasswordPage" Title="Redefinição de Senha">
    <ContentPage.Content>
        <StackLayout Margin="0,180,0,0">
            <Label Text="Informe seu email de cadastro." TextColor="Black" FontSize="16"  HorizontalOptions="Center"  />
            <Entry Text="{Binding Email}" VerticalOptions="Center" Margin="20,0" ClearButtonVisibility="WhileEditing"/>
            <!--<ActivityIndicator IsRunning="{Binding IsRunning}" IsVisible="{Binding IsRunning}"/>-->
            <Button Text="Enviar" HorizontalOptions="FillAndExpand" Margin="100,0" Command="{Binding ResetCommand}" Style="{StaticResource ButtonStyle}"/>
            <Label Text="Observação:" FontSize="14" TextColor="Black" Margin="0,60,0,0" Style="{StaticResource LabelStyle}" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center"/>
            <Label Text="Verifique a pasta de spam no seu email" HorizontalOptions="Center"
                   Style="{StaticResource LabelStyle}" FontSize="12" TextColor="Black" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>

